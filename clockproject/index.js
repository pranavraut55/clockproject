var marks = {
    ravi: "hello",
    shubham: 78,
    harry: "hello"
}
// let a = Object.values(marks);
// console.log(Object.values(marks).length);
// the var declaration
const cars = ["Saab", "Volvo", "BMW"];
let i=0;
let count=0;
// function that performs the task
function hideable(){
        if(count < cars.length){
        document.getElementById("ap").setAttribute("tabindex", "0");
    document.getElementById("ap").innerHTML
                = 'Focusable button';
                let btn =document.createElement("button",);
                count++;
document.body.appendChild(btn);
btn.setAttribute("class", "btn btn-outline-success");
btn.setAttribute("id", "btrn" +[i]);
btn.className="col";
let qw=cars[i];
btn.innerHTML=qw;
i=i+1;

}
// the error message if array values are finished
else
{
    let para = document.createElement("p",);
    document.body.appendChild(para);
    let pval= "End of values";
    para.innerHTML=pval;
}
// backtick implementation
// var el1 =document.getElementById("btrn0").innerHTML= `<button>${marks.harry}</button>`
}

